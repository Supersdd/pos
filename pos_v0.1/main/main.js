function printReceipt(inputs) {
  var result = '***<没钱赚商店>收据***\n';
  var temp = {};
  var temp2 = [];
  var sum = 0;
  inputs.forEach(function(val) {
    temp[val1.name] = temp[val1.name] ? temp[val1.name] + 1 : 1;
  })
